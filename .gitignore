# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist-ssr
*.local
s-out.txt
s-err.txt
serve-out.txt
serve-err.txt

# NOTE: dist/ is committed on purpose so Render can start even if vite build is skipped.
# Rebuild locally with `npm run build` before release commits when UI changes.

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
